eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/
spring: 
  application:
    name: demo-config-client
  cloud:
    config:
      uri: http://localhost:8888/
      profile: default
      label: master
server:
  port: 8889